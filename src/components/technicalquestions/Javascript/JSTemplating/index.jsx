const myFunction = () => {

    /*
        Javascript templating is simply jsx with backticks

        The backticks can handle string interpolation and multiline strings

        string interpolation meaning concatenating variables with strings
    */
    return (
        <div>

        </div>
    )
}